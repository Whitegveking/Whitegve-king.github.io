# 多态

**什么是多态**：事物的多种形态

**多态的表现形式**：`父类类型 对象名称 = 子类对象`

**多态的前提**：

- **有继承/实现关系**
- **有父类引用指向子类对象**
- 有方法的重写（可选）

**多态的好处**

1. 方法中使用父类型作为参数，可以接收父类对象+所有子类对象
2. 如果进行方法重写，利用多态调用方法，可以调用不同子类中重写的方法

```java
Fu f = new Zi();
```

```java
package polytests1;

public class Admin extends Person
{
    public Admin()
    {
        // super();
    }

    public Admin(String name, String username, String password)
    {
        super(name, username, password);
    }

    @Override
    public void work()
    {
        System.out.println("管理员需要管理");
    }
}
package polytests1;

public class Person
{
    private String name;
    private String username;
    private String password;

    public Person()
    {
    }

    public Person(String name, String username, String password)
    {
        this.name = name;
        this.username = username;
        this.password = password;
    }

    public String getName()
    {
        return name;
    }

    public void setName(String name)
    {
        this.name = name;
    }

    public String getUsername()
    {
        return username;
    }

    public void setUsername(String username)
    {
        this.username = username;
    }

    public String getPassword()
    {
        return password;
    }

    public void setPassword(String password)
    {
        this.password = password;
    }

    public void work()
    {
        System.out.println("每个人都需要工作");
    }
}
package polytests1;

public class Student extends Person
{
    public Student()
    {
    }

    public Student(String name, String username, String password)
    {
        super(name, username, password);
    }

    @Override
    public void work()
    {
        System.out.println("学生需要学习");
    }
}
package polytests1;

public class StudentManager
{
    // 注册用户，利用Person参数实现多态
    public boolean register(Person per)
    {
        System.out.println("姓名为" + per.getName() + "的账户注册成功，账号" + per.getUsername() + "，密码" + per.getPassword());
        per.work();
        return true;
    }
}
package polytests1;

public class Teacher extends Person
{
    public Teacher()
    {
    }

    public Teacher(String name, String username, String password)
    {
        super(name, username, password);
    }

    @Override
    public void work()
    {
        System.out.println("老师需要教学");
    }
}
package polytests1;

public class Test
{
    static void main()
    {
        /*
        学生类:
            属性:姓名，账号，密码
            行为:work:学生的工作是学习
        老师类:
            属性:姓名，账号，密码
            行为:work:老师的工作是教书
        管理员:
            属性:姓名，账号，密码
            行为:work:管理员的工作是管理网站
        学生管理系统:
            行为:注册register
            要求:能注册所有的角色
            输出:姓名为张三的同学注册成功，账号zhangsan，密码123456
        */
        StudentManager manager = new StudentManager();

        Student stu = new Student("张三", "zhangsan", "123456");

        Teacher t = new Teacher("李四", "lisi", "123456");

        Admin admin = new Admin("王五", "wangwu", "123456");

        manager.register(stu);
        manager.register(t);
        manager.register(admin);


    }
}

```

## 多态调用成员的特点

- **变量调用**：编译看左边，运行看左边
- **方法调用**：编译看左边，运行看右边

```java
package polytests2;

public class Fu
{
    String name = "Fu";
    public void fuShow()
    {
        System.out.println("fuShow");
    }
    public void show()
    {
        System.out.println("show");
    }
}
package polytests2;

public class Zi extends Fu
{
    String name = "Zi";

    public void ziShow()
    {
        System.out.println("ziShow");
    }

    @Override
    public void show()
    {
        System.out.println("show override");
    }
}
package polytests2;

public class Test
{
    static void main()
    {
        Fu f = new Fu();
        Zi z = new Zi();
        Fu fz = new Zi();

        f.show();
        f.fuShow();
        z.show();
        z.ziShow();
        fz.show();
        fz.fuShow();
    }

}

```

```cmd
"C:\JAVA tools\bin\java.exe" "-javaagent:C:\JAVA tools\IntelliJ IDEA 2026.2.0.1\lib\idea_rt.jar=53792" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\72982\IdeaProjects\polymorphism\out\production\polymorphism polytests2.Test
show
fuShow
show override
ziShow
show override
fuShow
```

### 类型转换

**多态的弊端**：多态下不能使用子类的独有功能

- **自动类型转换（从子到父）**：子类对象赋值给父类类型的变量
- **强制类型转换（从父到子）**：`子类 对象变量 = (子类)父类类型的变量`

**强制类型转换能解决什么问题？需要注意什么？**

- 可以转换成真正的子类类型，从而调用子类独有功能
- 转换类型与真实对象类型不一致会报错
- 转换的时候用`instanceof`关键字进行判断

**作用**：可以解决多态的弊端，可以实现调用子类独有的功能

### 抽象类和抽象方法

**抽象方法**：将共性的行为（方法）抽取到父类后。因为每一个子类的方法体是不一样的，所以，在父类中**不能确定具体的方法体**。该方法就可以定义为抽象方法。

**抽象类**：如果一个类中**存在抽象方法**，那么该类就**必须声明**为抽象类

#### 定义格式

**抽象方法**

​	`public abstract 返回值类型 方法名(参数列表);`

**抽象类**

​	`public abstract class 类名{...}`

#### 注意事项

1. 抽象类不能实例化，如果可以创建抽象类的对象，用对象调用一个没有方法体的抽象方法，没有任何意义
2. 抽象类中不一定有抽象方法，为了不让外界创建本类的对象；有抽象方法的类一定是抽象类
3. 抽象类中可以有构造方法，是`new`关键字用于创建对象而非构造方法；构造方法用于给成员变量进行赋值
4. 抽象类的子类，要么重写抽象类中的所有抽象方法；要么子类也是抽象类

```java
package polytests03;

public abstract class Animal
{
    /*Animal类(抽象类):属性:名字，颜色
    行为:eat()抽象方法，喝水*/
    String name;
    String color;

    public Animal()
    {
    }

    public Animal(String name, String color)
    {
        this.name = name;
        this.color = color;
    }

    public void drink()
    {
        System.out.println("drink water");
    }

    public abstract void eat();
}
package polytests03;

public class Cat extends Animal
{
    /*猫类:
    行为:猫吃鱼，猫抓老鼠*/

    public Cat()
    {
    }

    public Cat(String name, String color)
    {
        super(name, color);
    }

    @Override
    public void eat()
    {
        System.out.println("cat eat fish");
    }

    public void catchMouse()
    {
        System.out.println("catch mouse");
    }
}
package polytests03;

public class Dog extends Animal
{
    /*狗类:
    行为:狗吃骨头，狗看家*/

    public Dog()
    {
    }

    public Dog(String name, String color)
    {
        super(name, color);
    }

    @Override
    public void eat()
    {
        System.out.println("dog eat bone");
    }

    public void guard()
    {
        System.out.println("guard home");
    }
}
package polytests03;

public class Test
{
    public static void main(String[] args)
    {
        Dog a = new Dog("大黄", "黄色");
        Cat b = new Cat("小花", "白色");

        a.guard();
        b.catchMouse();

        a.eat();
        a.drink();
        b.eat();
        b.drink();
    }
}

```

```cmd
"C:\JAVA tools\bin\java.exe" "-javaagent:C:\JAVA tools\IntelliJ IDEA 2026.2.0.1\lib\idea_rt.jar=64398" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\72982\IdeaProjects\polymorphism\out\production\polymorphism polytests03.Test
guard home
catch mouse
dog eat bone
drink water
cat eat fish
drink water
```

### 抽象类和抽象方法的意义

给出一个统一的格式，包括函数名、形参等，强制子类必须按照这种格式进行重写，方便调用和查看格式