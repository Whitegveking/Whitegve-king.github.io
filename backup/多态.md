# 多态

**什么是多态**：事物的多种形态

**多态的表现形式**：`父类类型 对象名称 = 子类对象`

**多态的前提**：

- **有继承/实现关系**
- **有父类引用指向子类对象**
- 有方法的重写（可选）

**多态的好处**

1. 方法中使用父类型作为参数，可以接收父类对象+所有子类对象
2. 如果进行方法重写，利用多态调用方法，可以调用不同子类中重写的方法

```java
Fu f = new Zi();
```

```java
package polytests1;

public class Admin extends Person
{
    public Admin()
    {
        // super();
    }

    public Admin(String name, String username, String password)
    {
        super(name, username, password);
    }

    @Override
    public void work()
    {
        System.out.println("管理员需要管理");
    }
}
package polytests1;

public class Person
{
    private String name;
    private String username;
    private String password;

    public Person()
    {
    }

    public Person(String name, String username, String password)
    {
        this.name = name;
        this.username = username;
        this.password = password;
    }

    public String getName()
    {
        return name;
    }

    public void setName(String name)
    {
        this.name = name;
    }

    public String getUsername()
    {
        return username;
    }

    public void setUsername(String username)
    {
        this.username = username;
    }

    public String getPassword()
    {
        return password;
    }

    public void setPassword(String password)
    {
        this.password = password;
    }

    public void work()
    {
        System.out.println("每个人都需要工作");
    }
}
package polytests1;

public class Student extends Person
{
    public Student()
    {
    }

    public Student(String name, String username, String password)
    {
        super(name, username, password);
    }

    @Override
    public void work()
    {
        System.out.println("学生需要学习");
    }
}
package polytests1;

public class StudentManager
{
    // 注册用户，利用Person参数实现多态
    public boolean register(Person per)
    {
        System.out.println("姓名为" + per.getName() + "的账户注册成功，账号" + per.getUsername() + "，密码" + per.getPassword());
        per.work();
        return true;
    }
}
package polytests1;

public class Teacher extends Person
{
    public Teacher()
    {
    }

    public Teacher(String name, String username, String password)
    {
        super(name, username, password);
    }

    @Override
    public void work()
    {
        System.out.println("老师需要教学");
    }
}
package polytests1;

public class Test
{
    static void main()
    {
        /*
        学生类:
            属性:姓名，账号，密码
            行为:work:学生的工作是学习
        老师类:
            属性:姓名，账号，密码
            行为:work:老师的工作是教书
        管理员:
            属性:姓名，账号，密码
            行为:work:管理员的工作是管理网站
        学生管理系统:
            行为:注册register
            要求:能注册所有的角色
            输出:姓名为张三的同学注册成功，账号zhangsan，密码123456
        */
        StudentManager manager = new StudentManager();

        Student stu = new Student("张三", "zhangsan", "123456");

        Teacher t = new Teacher("李四", "lisi", "123456");

        Admin admin = new Admin("王五", "wangwu", "123456");

        manager.register(stu);
        manager.register(t);
        manager.register(admin);


    }
}

```

## 多态调用成员的特点

- **变量调用**：编译看左边，运行看左边
- **方法调用**：编译看左边，运行看右边

```java
package polytests2;

public class Fu
{
    String name = "Fu";
    public void fuShow()
    {
        System.out.println("fuShow");
    }
    public void show()
    {
        System.out.println("show");
    }
}
package polytests2;

public class Zi extends Fu
{
    String name = "Zi";

    public void ziShow()
    {
        System.out.println("ziShow");
    }

    @Override
    public void show()
    {
        System.out.println("show override");
    }
}
package polytests2;

public class Test
{
    static void main()
    {
        Fu f = new Fu();
        Zi z = new Zi();
        Fu fz = new Zi();

        f.show();
        f.fuShow();
        z.show();
        z.ziShow();
        fz.show();
        fz.fuShow();
    }

}

```

```cmd
"C:\JAVA tools\bin\java.exe" "-javaagent:C:\JAVA tools\IntelliJ IDEA 2026.2.0.1\lib\idea_rt.jar=53792" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\72982\IdeaProjects\polymorphism\out\production\polymorphism polytests2.Test
show
fuShow
show override
ziShow
show override
fuShow
```

### 类型转换

**多态的弊端**：多态下不能使用子类的独有功能

- **自动类型转换（从子到父）**：子类对象赋值给父类类型的变量
- **强制类型转换（从父到子）**：`子类 对象变量 = (子类)父类类型的变量`

**强制类型转换能解决什么问题？需要注意什么？**

- 可以转换成真正的子类类型，从而调用子类独有功能
- 转换类型与真实对象类型不一致会报错
- 转换的时候用`instanceof`关键字进行判断

**作用**：可以解决多态的弊端，可以实现调用子类独有的功能

### 抽象类和抽象方法

**抽象方法**：将共性的行为（方法）抽取到父类后。因为每一个子类的方法体是不一样的，所以，在父类中**不能确定具体的方法体**。该方法就可以定义为抽象方法。

**抽象类**：如果一个类中**存在抽象方法**，那么该类就**必须声明**为抽象类

#### 定义格式

**抽象方法**

​	`public abstract 返回值类型 方法名(参数列表);`

**抽象类**

​	`public abstract class 类名{...}`

#### 注意事项

1. 抽象类不能实例化，如果可以创建抽象类的对象，用对象调用一个没有方法体的抽象方法，没有任何意义
2. 抽象类中不一定有抽象方法，为了不让外界创建本类的对象；有抽象方法的类一定是抽象类
3. 抽象类中可以有构造方法，是`new`关键字用于创建对象而非构造方法；构造方法用于给成员变量进行赋值
4. 抽象类的子类，要么重写抽象类中的所有抽象方法；要么子类也是抽象类

```java
package polytests03;

public abstract class Animal
{
    /*Animal类(抽象类):属性:名字，颜色
    行为:eat()抽象方法，喝水*/
    String name;
    String color;

    public Animal()
    {
    }

    public Animal(String name, String color)
    {
        this.name = name;
        this.color = color;
    }

    public void drink()
    {
        System.out.println("drink water");
    }

    public abstract void eat();
}
package polytests03;

public class Cat extends Animal
{
    /*猫类:
    行为:猫吃鱼，猫抓老鼠*/

    public Cat()
    {
    }

    public Cat(String name, String color)
    {
        super(name, color);
    }

    @Override
    public void eat()
    {
        System.out.println("cat eat fish");
    }

    public void catchMouse()
    {
        System.out.println("catch mouse");
    }
}
package polytests03;

public class Dog extends Animal
{
    /*狗类:
    行为:狗吃骨头，狗看家*/

    public Dog()
    {
    }

    public Dog(String name, String color)
    {
        super(name, color);
    }

    @Override
    public void eat()
    {
        System.out.println("dog eat bone");
    }

    public void guard()
    {
        System.out.println("guard home");
    }
}
package polytests03;

public class Test
{
    public static void main(String[] args)
    {
        Dog a = new Dog("大黄", "黄色");
        Cat b = new Cat("小花", "白色");

        a.guard();
        b.catchMouse();

        a.eat();
        a.drink();
        b.eat();
        b.drink();
    }
}

```

```cmd
"C:\JAVA tools\bin\java.exe" "-javaagent:C:\JAVA tools\IntelliJ IDEA 2026.2.0.1\lib\idea_rt.jar=64398" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\72982\IdeaProjects\polymorphism\out\production\polymorphism polytests03.Test
guard home
catch mouse
dog eat bone
drink water
cat eat fish
drink water
```

### 抽象类和抽象方法的意义

给出一个统一的格式，包括函数名、形参等，强制子类必须按照这种格式进行重写，方便调用和查看格式

## 接口

**接口**：接口就是一个规则，而且是独立于继承体系以外的规则（可以理解为干爹）

**格式**：用关键词`interface`，`public interface 接口名{...}`

**使用**：接口和类之间是实现关系，通过`implements`关键字表示，`public class 类名 implements 接口{...}`

**注意点**

- 接口不能实例化
- 接口的子类（实现类），要么重写接口中的所有抽象方法，要么实现类是一个抽象类
- 一个类可以实现多个接口，也可以在继承一个类的同时，实现多个接口
- `public class 类名 implements 接口1,接口2{...}`
- `public class 类名 extends 父类 implements 接口1,接口2{...}`

### 接口中成员的特点

**成员变量**：只能是常量。默认修饰符：`public static final`

**构造方法**：无

**成员方法**：只能是抽象方法。默认修饰符：`public abstract`

### 接口和类之间的关系

**类和类之间的关系**

- 继承关系，只能单继承，不能多继承，但不能多层继承

**类和接口之间的关系**

- 实现关系，可以单实现，可以多实现，还可以在继承一个类的同时实现多个接口
- 如果父类也是一个抽象类的话，子类中需要把来自继承和实现的所有抽象方法进行重写，要么子类本身也是一个抽象类
- 如果多个接口中出现了重复的抽象方法，此时我们只需要重写一次就行了

**接口和接口之间的关系**

- 继承关系，可以单继承，也可以多继承
- 如果有一个接口A继承了多个接口，此时相当于是把多个接口中的抽象方法全部继承下来了，之后实现类实现接口A的时候，就要把所有的抽象方法进行重写

<img width="1581" height="693" alt="Image" src="https://github.com/user-attachments/assets/2fd21215-c3c4-418d-bf93-37be6eea212d" />

```java
package polytests05;

public class Person
{
    private String name;
    private int age;

    public Person()
    {
    }

    public Person(String name, int age)
    {
        this.name = name;
        this.age = age;
    }

    public String getName()
    {
        return name;
    }

    public void setName(String name)
    {
        this.name = name;
    }

    public int getAge()
    {
        return age;
    }

    public void setAge(int age)
    {
        this.age = age;
    }
}

package polytests05;

public abstract class Athlete extends Person
{
    public abstract void learn();
}

package polytests05;

public abstract class Coach extends Person
{
    public abstract void teach();
}
package polytests05;

public interface English
{

    void speakEnglish();
}

package polytests05;

public class PpAthlete extends Athlete implements English
{

    @Override
    public void learn()
    {
        System.out.println("learning pingpong");
    }

    @Override
    public void speakEnglish()
    {
        System.out.println("pingpong athlete can speak English");
    }
}

package polytests05;

public class PpCoach extends Coach implements English
{
    @Override
    public void teach()
    {
        System.out.println("teaching pingpong");
    }

    @Override
    public void speakEnglish()
    {
        System.out.println("pingpong coach can speak English");
    }
}

```

### 接口的新特性

- **JDK7及以前**:接口中只能定义抽象方法
- **JDK8新特性**:为了接口升级时向下兼容，接口中可以定义有方法体的方法(默认、静态)。默认方法用`default`修饰，子类可以不重写默认方法，如果多个接口中存在相同名字的默认方法，子类就必须对该方法进行重写。
- **JDK9新特性**:为了抽取重复代码，接口中可以定义私有方法(私有普通方法，私有静态方法)，只为本接口使用

## 内部类

**内部类**：在一个类的里面再定义一个类

**用处**：如果一个类表示的事物是一个类的一部分，且单独存在时没有任何意义

### 内部类的分类

**成员内部类**

写在成员位置的，属于外部类的成员

- 获取成员内部类对象的方式一:当成员内部类被private修饰时。在外部类编写方法，对外提供内部类对象
- 获取成员内部类对象的方式二:当成员内部类被非私有修饰时，直接创建对象。`Outer.Inner oi= new Outer().new Inner();`

<img width="1593" height="905" alt="Image" src="https://github.com/user-attachments/assets/a9a00645-d980-41a0-9336-862f4c10323b" />

```java
package innerclassdemo01;

public class Outer
{
    private int a = 10;
    class Inner
    {
        private int a =20;
        public void show()
        {
            int a = 30;
            // Outer.this获取了外部类对象的地址值
            System.out.println(Outer.this.a);
            System.out.println(this.a);
            System.out.println(a);
        }
    }
}
```

```cmd
"C:\JAVA tools\bin\java.exe" "-javaagent:C:\JAVA tools\IntelliJ IDEA 2026.2.0.1\lib\idea_rt.jar=58554" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\72982\IdeaProjects\innerclass\out\production\innerclass innerclassdemo01.Test
10
20
30
```



**静态内部类**

- 静态内部类只能访问外部类中的静态变量和静态方法
- 如果在静态内部类中，想要访问外部类非静态的内容，需要创建外部类的对象
- `外部类名.内部类名 对象名 = new 外部类名.内部类名();`
- 先创建对象，用对象调用静态内部类中非静态方法

**局部内部类**

==**匿名内部类**==
- 一个没有名字的内部类
- **作用**：在继承一个类或者实现一个接口的时候少写一个文件，也是lambda的前置知识点

**定义格式**：

<img width="975" height="155" alt="Image" src="https://github.com/user-attachments/assets/ab671166-1142-4d09-9a91-f65bb23ecead" />

匿名内部类的定义格式 = 没有名字的java类 + 继承/实现 + 重写方法 + 创建对象

最终也是可以理解为是一个**没有名字的java类的对象**

```java
package innerclassdemo02;

public interface Swim
{
    public abstract void swim();
}
package innerclassdemo02;

public class Test
{
    public static void goSwim(Swim s)
    {
        s.swim();
    }
    static void main()
    {
        Swim s = new Swim()
        {
            @Override
            public void swim()
            {
                System.out.println("someone is swimming");
            }
        };
        goSwim(s);
    }
}

```

```cmd
"C:\JAVA tools\bin\java.exe" "-javaagent:C:\JAVA tools\IntelliJ IDEA 2026.2.0.1\lib\idea_rt.jar=49670" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\72982\IdeaProjects\innerclass\out\production\innerclass innerclassdemo02.Test
someone is swimming
```

